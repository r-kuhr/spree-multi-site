<%= error_messages_for :site %>
<h1><%= t("editing_site") %></h1>
<br/>
<% form_for(:site, :url => object_url, :html => { :method => :put }) do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
  <p>
    <%=submit_tag t("update") %>
  </p>
<% end %>
